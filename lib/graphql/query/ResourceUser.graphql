query ResourceUser($id: String!) {
  ChatId(userId: $id) {
    person {
      resourceUsers {
        id
        credential {
          id
        }
      }
      gitHubId {
        login
      }
    }
  }
}
