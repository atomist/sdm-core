query ResourceUser($id: String!) {
    ChatId(userId: $id) {
        person {
            resourceUsers {
                id
            }
            gitHubId {
                login
            }
        }
    }
}
