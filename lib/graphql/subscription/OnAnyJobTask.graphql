subscription OnAnyJobTask($registration: String) {
    AtmJobTask(owner: $registration) {
        id
        data
        name
        job {
            id
            data
        }
        state
        message
    }
}
